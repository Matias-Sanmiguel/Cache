/**
 * Clase encargada de representar la conexión con Cassandra.
 *
 * En esta primera versión no se conecta realmente a la base.
 * Sirve como estructura inicial para mostrar dónde iría la conexión.
 */
public class CassandraClient {

    /**
     * Simula la conexión a Cassandra.
     * Más adelante acá se configuraría el host, puerto y keyspace.
     */
    public static void connect() {
        System.out.println("Conectando a Cassandra...");
        System.out.println("Keyspace usado: cache_ks");
    }
}